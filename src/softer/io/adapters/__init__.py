"""Source adapters: raw sensor/network records -> common schema (Sect. 2.1.1).

Adding a new data source means writing one adapter here and one YAML file under
``configs/networks/``; nothing else in the pipeline changes.
"""
